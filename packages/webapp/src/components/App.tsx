import { Route, Switch } from "react-router-dom";

import Core from "./core/Core";

const App = () => {
  return (
    <Core>
      <Switch>
        <Route path="" component={} />
      </Switch>
    </Core>
  );
};

export default App;
